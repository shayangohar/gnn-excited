dataset:
  hdf5_path: data/A_9.hdf5
  manifest_path: data/processed/a9_manifest_10000.csv
  max_manifest_molecules: 10000
  split_seed: 17
  train_fraction: 0.8
  val_fraction: 0.1

targets:
  energy: S1_eV
  oscillator: log1p_S1_f

model:
  hidden_channels: 64
  out_channels: 2
  num_blocks: 2
  int_emb_size: 32
  basis_emb_size: 8
  out_emb_channels: 128
  num_spherical: 3
  num_radial: 6
  cutoff: 5.0
  max_num_neighbors: 32

training:
  batch_size: 16
  epochs: 25
  learning_rate: 0.0005
  weight_decay: 0.0
  device: cuda
  checkpoint_path: checkpoints/dimenetpp_a9_10k_gpu.pt
  metrics_csv_path: runs/dimenetpp_a9_10k_gpu.metrics.csv
  summary_json_path: runs/dimenetpp_a9_10k_gpu.summary.json
